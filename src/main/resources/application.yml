spring:
  datasource:
    driver-class-name: org.h2.Driver
    url : jdbc:h2:tcp://localhost/~/spring-data-envers
    username: sa
    password:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create